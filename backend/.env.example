# ConektaBots Backend - Variáveis de Ambiente
# Copie este arquivo para .env e substitua os valores

# App
APP_NAME=ConektaBots
DEBUG=false

# Database (SQLite para dev, PostgreSQL para prod)
DATABASE_URL=sqlite:///./data/database.db

# JWT
SECRET_KEY=CHANGE-ME-USE-secrets-token-urlsafe-32
ACCESS_TOKEN_EXPIRE_MINUTES=30
REFRESH_TOKEN_EXPIRE_DAYS=7

# CORS (lista separada por vírgula no .env, ou JSON)
CORS_ORIGINS=["http://localhost:5173","http://localhost:3000"]

# Timezone
TIMEZONE=America/Sao_Paulo
